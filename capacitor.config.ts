import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.feliziolee.queer-health',
  appName: 'queer-health',
  webDir: 'dist/queer-health'
};

export default config;
